{
  "name": "ssps-generator",
  "version": "1.0.0",
  "description": "Generování písemných zkoušení a úkolů",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "node src/."
  }
}
